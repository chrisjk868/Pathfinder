class UnionFind {

    constructor(r, c) {
        this.parent = Array(r * c).fill().map((_, idx) => idx);
        this.rank = Array(r * c).fill(1);
    }

    find() {
        
    }

    union() {

    }

}